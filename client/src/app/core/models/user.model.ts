/**
 * Matches the User shape.
 */
export interface User {
  id: string;
  name: string;
  email: string;
  companyName: string;
  addressCity: string;
  addressStreet: string;
  createdAt: string;
  updatedAt: string;
}
